apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: iceberg-data-pvc
  namespace: replay-system
  labels:
    app.kubernetes.io/part-of: replay-system
    app.kubernetes.io/component: datalake
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
