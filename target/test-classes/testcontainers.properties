# Testcontainers configuration for this project.
# Ryuk is disabled here because it fails on rootless Docker runtimes (e.g. Colima).
# Containers are cleaned up automatically when the JVM exits via shutdown hooks.
ryuk.disabled=true
